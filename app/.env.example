# Copy to .env if you want to override the defaults.
#
# From the host, the application database is on localhost:5432.
# From inside a container on the compose network, it is db:5432.

DATABASE_URL=postgres://postgres:postgres@localhost:5432/chp99
PORT=3000
